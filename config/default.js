const config = {
  // 启动端口
  port: 8000,

  // 数据库配置
  database: {
    DATABASE: 'koa_demo',
    PORT: 3306,
    USERNAME: 'root',
    PASSWORD: 'graceji',
    HOST: 'localhost',
  }
}

module.exports = config;